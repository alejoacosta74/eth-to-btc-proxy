# run integration tests
# Usage: integration.sh [testname]
#   testname: name of the test to run, if not specified, run all tests
#   -h: print this help message
#   -v: verbose mode
#   -d: debug mode
#   -n: dry run mode
#   -s: skip cleanup
#   -c: cleanup only
#   -t: test only
#   -p: print test name only
#!/bin/bash
